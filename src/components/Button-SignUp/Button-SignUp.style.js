import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        borderRadius: 5,
        padding: 10,
        margin: 5,
        marginLeft:250,
        alignItems: 'flex-end',
        justifyContent: 'center',
        width: 150,
    },
    text:{
        fontSize:20,
        color: 'white',
    },
});